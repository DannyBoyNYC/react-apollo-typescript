// import { ITheme } from '../../theme.config';

export const styles = theme => ({
  root: {
    display: 'block',
    marginBottom: '1.5rem',
    paddingTop: '0.5rem',
    borderTop: '1px dotted #bbb',
  },
});
