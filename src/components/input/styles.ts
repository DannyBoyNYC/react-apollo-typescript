import { ITheme } from '../../theme.config';

export const styles = (theme: ITheme) => ({
  root: {
    display: 'block',
    marginBottom: '0.75rem',
    width: '320px',
    height: '1.5rem',
    padding: '0.25rem',
  },
});
